import type { Server } from "@prisma/client";
import { prisma } from "~/db.server";

/*export async function getServer({ id }: Pick<Server, "id">) {
  return prisma.server.findFirst({ where: { id } });
}*/
